import sys

# Read in the first line
line=sys.stdin.readline()

#While there are lines remaining
while line:
	# Print out the line (followed by a newline)
	print(line)
	# Read in the next line
	line=sys.stdin.readline()